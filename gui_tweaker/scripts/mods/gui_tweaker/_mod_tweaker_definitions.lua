-- Mod Tweaker scenegraph + widget factories — stub.
-- v0.1 scaffold placeholder. Task #6 imports the scenegraph skeleton
-- (root → dim layer → main panel → tab strip → 1400x746 list mask → scroll
-- area) from VT2's options_view_definitions.lua; tasks #7-8 add widget
-- factories (slider :1569, checkbox :1304, dropdown, stepper, keybind, plus
-- VT2 extras: color picker, text input, dividers, depends_on). Empty for now.

return {
    scenegraph_definition = {},
    widget_factories = {},
}
