> [!WARNING]
> ⚠ **SUPERSEDED** — this snapshot is from 2026-05-22 (50 days old).
> Recent state may differ. Kept for historical context — verify against current
> code before acting on findings. Remove this banner manually after a refresh
> or move the doc to `_archive/audits/2026-05-22/`.
# Test coverage audit per §15 — 2026-05-24

PROJECT_STANDARDS §15: "Every bug requires a test." Audit of every fix shipped 2026-05-22 → 2026-05-24.

Source-of-truth checked: each mod's `<mod>/scripts/mods/<mod>/<mod>.lua` (`_rt_register("name", ...)` registrations) plus `<mod>/CHANGELOG.md` most-recent entries. Lint coverage cross-referenced against `tools/lint/regression-lint.ps1` (which has self-tests for `forward-ref`, `unescaped-percent`, `strict-table-lookup`, `late-local-shadows-global`).

## Summary

- Total fixes audited: 18
- PASS: 9
- PARTIAL: 5
- MISSING: 2
- N/A: 2

## PASS (test present, CHANGELOG documents it)

| Mod | Version | Bug | Test(s) found | Notes |
|---|---|---|---|---|
| cosmetics_tweaker | v0.9.10-dev (per-mount illusion picker) | per-hand offhand pool shape | `offhand_options_per_hand_shape` (line 6752) | Test rejects legacy flat-array shape; documented at top of file. |
| cosmetics_tweaker | v0.9.9.x (UIUtils icon hook revert) | `get_ui_information_from_item` regression | `uiutils_hook_NOT_installed_pending` (line 6689) | Embeds revert marker constant; doctrine-compliant source-pattern check. |
| cosmetics_tweaker | v0.9.8.x (character mismatch gate) | cross-skeleton patching | `character_mismatch_gate_in_apply_la` (line 6733) | Marker + function existence check. |
| ct | v0.7.94+ (dormant boons disable) | runtime crash class | `dormant_boons_NOT_registered` (7184), `dormant_boons_NOT_in_pool` (7213), `dormant_boon_rarity_global_absent` (7177), `dormant_setting_keys_not_consumed` (7250), `dormant_chat_commands_removed` (7263) | 5-check fence; CHANGELOG v0.7.100 explicitly lists all 5 names. |
| ct | v0.7.95-dev (Starting Coins setter) | 300-gives-500 bug | `starting_coins_setter_not_adder` (7479), `starting_coins_value_matches_setting` (7494) | Both names exactly as expected. |
| ct | v0.7.96-dev (Miracle of Isha hardening) | mutex select | `miracle_of_isha_choice_widget_is_dropdown` (7528), `miracle_of_isha_titles_present` (7556), `miracle_of_isha_hook_installed` (7578) | All three expected names exist. |
| ct | v0.7.97-dev (Engineer bombs blocklist) | world-spawn block | `engineer_bombs_not_in_world_spawns` (7591), `engineer_bombs_present_in_vanilla_pickups` (7619) | Both names exactly as expected. |
| ct | v0.7.99/.100-dev (DORMANT_BOON_RARITY scope) | global-absent | `dormant_boon_rarity_global_absent` (7177) | Expected name was `dormant_boon_rarity_is_table`; v0.7.100 full purge renamed it (now PASS means the global is absent). CHANGELOG v0.7.100 documents the rename + inversion. |
| ct | v0.7.102-dev (max_energy network bound) | consumption-side rewrite | `ct_meta_ammo_uses_consumption_side` (7658), `ct_clamp_helper_present` (7687), `ct_no_direct_max_energy_mutation` (7711) | All three expected names exist; CHANGELOG documents each. |

## PARTIAL (test exists somewhere, CHANGELOG gap or partial coverage)

| Mod | Version | Bug | What was found | Gap |
|---|---|---|---|---|
| cosmetics_tweaker | v0.9.10-dev (glow auto-popup) | popup-on-wield invariant | No specific `_rt_register` named for glow-popup; the auto-popup table reset at line 687 is the runtime guard | CHANGELOG v0.9.10 is purely a version-reset entry; doesn't list any popup-related regression check. Recommend: add `glow_popup_gate_present` source-pattern check (marker `glow_picker_auto_popup_enabled`) and document in next CHANGELOG entry. |
| wt | v0.12.72-dev (ItemMasterList rawget GH #8) | strict-lookup fix on 5 sites | Covered by `tools/lint/regression-lint.ps1` `strict-table-lookup` check (self-tested at lint script lines 1147–1167) | No mod-level `_rt_register` for these specific sites. CHANGELOG documents the conversion + names the lint as the verification step (line 16-20). Lint coverage is doctrine-compliant per §15 ("forward-ref / late-local-shadows-global … count as tests if they're proven to fire"). |
| gt | v0.2.47-dev (NetworkLookup rawget) | `_gt_is_spawn` strict-lookup site | Lint `strict-table-lookup` covers it (CHANGELOG points to `tools/lint/regression-lint.ps1`) | No `_rt_register` runtime check added for this site. The runtime guard "echoes 'Unknown pickup name' and returns" is itself only exercised at chat-command time. PARTIAL because the conversion is lint-covered but no runtime-state check. |
| crt | v0.3.9-dev (NetworkLookup rawget on big_rebalance) | latent strict-`__index` | Lint `strict-table-lookup` + pre-existing `rawget_on_buff_templates_marker` (line 385) embeds the doctrine comment | The pre-existing marker is generic ("must use rawget(t, key) for existence checks") — it doesn't target the specific big_rebalance site touched in v0.3.9. Lint is the load-bearing test. |
| et | v0.5.6-dev (NetworkLookup rawget × 6 sites) | strict-`__index` on hit_zones/damage_sources | Lint `strict-table-lookup` covers all six sites | No mod-level `_rt_register` added. CHANGELOG cites the lint as verification (`regression-lint.ps1 -Quiet — sites no longer appear in strict-table-lookup findings`). PARTIAL on the runtime-state-check axis — only the lint runs against these sites. |
| cwv | v0.1.330-dev (NetworkLookup rawget × 3 sites) | strict-`__index` on RPC handlers | Lint `strict-table-lookup` covers all three sites | CWV's CHANGELOG v0.1.329 claims an `/regression_test` chat command was added with 4 checks, but the source file `character_weapon_variants.lua` contains NO `_rt_register` framework and NO `mod:command("regression_test"` registration. PARTIAL because the lint catches the regression but the in-mod runtime smoke-checks promised in CHANGELOG v0.1.329 never actually shipped. |

## MISSING (no test for this fix — doctrine violation)

| Mod | Version | Bug | Recommendation |
|---|---|---|---|
| ct | v0.7.92-dev (Reckless Swings name-based lookup, GH #5) | tweak used hard-coded array indices; v0.7.84 added bail-safe; v0.7.92 made it work via name-based search | Add `_rt_register("reckless_swings_name_based_lookup", function() ... end)` that asserts `_find_entry_by` exists AS A FUNCTION at file scope, and that `reckless_swings_originals` (when the tweak is active) stores numeric `buff_index` / `dv_threshold_index` / `dv_damage_index` fields. Source-pattern check on the comment marker `name-based lookup` would also work. CHANGELOG v0.7.92 lists six verification steps, all manual; no automated check. |
| cwv | v0.1.329-dev (claimed `/regression_test` command) | CHANGELOG documents 4 in-game smoke checks (`cwv_variant`, `entry.name` non-clobber, ammo-bearing skin mirroring, InventoryPackageList path checks) | Either (a) actually ship the `mod:command("cwv_regression_test", ...)` with the four checks as promised, or (b) edit CHANGELOG v0.1.329 to remove the false claim. Recommend (a) — match the framework other mods use. The four conditions are already documented per-feedback-memory; converting them to runtime checks is mechanical. |

## N/A (no test required)

| Mod | Version | Reason |
|---|---|---|
| ct | v0.7.93-dev (Skulls boons) | Feature subsequently disabled in v0.7.98; the disable is covered by the dormant_boons_NOT_registered test family. Per §15: disabled features need a "disable-mode check" — which is exactly what `dormant_boons_NOT_registered` does for the Skulls block too (it walks `CT_DISABLED_DORMANT_BOON_NAMES`). |
| cim | (no dedicated version) % escape in localization | The localization file `crafting_in_modded_localization.lua` already uses `%%` escapes (verified at lines 36 and 39). Lint check `unescaped-percent` (regression-lint.ps1 lines 487–519, self-tested at 1097–1114) catches future regressions. No mod-level fix-version was published for this; lint is the source-of-truth. |
| VMBLauncher v0.5.0 | C# title auto-rewrite | xUnit tests already verified (`tools/vmb-launcher/tests/TitleVersionSyncTests.cs`). |

## Lint coverage cross-reference

`tools/lint/regression-lint.ps1` has self-tests proving each check fires on a positive fixture and stays silent on a negative fixture (lines 1078–1170):

- `forward-ref` — self-test at 1085-1095 (covers Lua forward-reference bugs)
- `unescaped-percent` — self-test at 1097-1114 (covers `%` escape in localization)
- `strict-table-lookup` — self-test at 1147-1167 (covers `NetworkLookup.*[key]` outside rawget)
- `late-local-shadows-global` — self-test at the corresponding section

All four lints are proven to fire on the patterns. Per §15, lint coverage counts as a test for source-pattern bugs.

## Recommendation

**Two immediate gaps to close** (MISSING bucket):

1. **cwv v0.1.330 follow-up**: ship the `/cwv_regression_test` command CHANGELOG v0.1.329 already announced. Source the four checks from the feedback-memory references (`feedback_cwv_clone_name_clobber.md`, `feedback_cwv_ammo_unit_required.md`). Time: ~30 min — the conditions are already documented.

2. **ct v0.7.92 (Reckless Swings)**: add a single `_rt_register("reckless_swings_name_based_lookup", ...)` source-pattern check that proves the comment marker `name-based lookup` shipped and `_find_entry_by` exists. Time: ~5 min. This is the only "live bug fix" that has zero automated test — all other fixes today are either lint-covered or have an `_rt_register` entry.

**PARTIAL bucket can wait for next touch** — the rawget conversions are all lint-covered, and §15 explicitly endorses lint-as-test for source-pattern checks. Adding runtime-state checks (e.g. that the strict metatable behaves as expected on a known-bad key) would be belt-and-suspenders but isn't required.

**One CHANGELOG drift to correct**: cosmetics_tweaker v0.9.10-dev's CHANGELOG entry is purely a version-reset note and doesn't document the glow auto-popup feature claimed in the audit prompt. Either the prompt's attribution is off (the auto-popup actually shipped in v0.9.9.3 per source comment at line 683) or the v0.9.10 CHANGELOG should be extended. Verify before changing.
