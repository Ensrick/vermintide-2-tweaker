# Engine reference set - improvement backlog (merged, prioritized)

Merged and deduplicated from the eleven lane docs' "implications for our mods" sections
(2026-07-11). Feeds the OOP professionalization program (`docs/OOP_REFACTOR_PLAN.md`).

Priorities: **P0** = correctness / crash-adjacent, **P1** = architecture / maintainability,
**P2** = polish. Rows are actionable standalone; when one ships, delete the row and cite
the fix at the owning doc's "implications" section (README maintenance rule 3).
Rows marked **(in flight: <agent>)** have an active session working the same issue -
coordinate before starting. Remember the dev-only edit doctrine: stable dirs are
read-only; promotion items say so explicitly.

## P0 - correctness / crash-adjacent

| Pri | Owner mod | Our file:line | Change | Engine citation |
|---|---|---|---|---|
| P0 | cosmetics_tweaker | `cosmetics_tweaker/scripts/mods/cosmetics_tweaker/_material_hijack_embedded.lua:161` | Material Hijack loads mat packages under the engine's own ref `"global"`, +1 refcount per spawned unit, zero unloads (issue #282, verified). Use own ref `"cosmetics_tweaker_mh"` + load-once gate (has_loaded/is_loading, or a loaded-set like `_preloaded_offhand_packages` at `cosmetics_tweaker.lua:2044/2086`). Diagnose first with `script_data.package_debug` + `Managers.package:dump_reference_counter(ref)` before building any custom ledger. (in flight: fix-cosmetics-282-421) | `package_manager.lua:26-27` (every load = +1), `:254-279` (shutdown drain/deadlock warning); `boot.lua:1759-1781` (`"global"` is the engine's ref); gate idiom `enemy_package_loader.lua:372-375` |
| ~~P0~~ RESOLVED v0.7.241-dev (#356) | ct_dev | `chaos_wastes_tweaker_dev.lua:5300` | FIXED: the no_roamers crash guard hooked STATIC `MutatorHandler.tweak_pack_spawning_settings` with a spurious `self` param, shifting every arg (the `difficulty_overrides` predicate always read nil, and the real `zone_mutator_list` was forwarded UNFILTERED - the Belakor `pairs(nil)` host CTD guid 4c84c68a could still fire). Dropped `self`, bound the 4 real params in vanilla order, filter BOTH lists, kept the predicate. Behavioral arity lock `/ct_regression_test no_roamers_strip_arity_356`; marker `CT_NO_ROAMERS_ARITY_FIX_MARKER`. | `mutator_handler.lua:748` (static def); `main_path_spawning_generator.lua:327` (dot call, 4 args); `mutator_no_roamers` `pairs(pack_spawning_settings.difficulty_overrides)` |
| P0 | crt + ct_dev | `career_tweaker/scripts/mods/career_tweaker/career_tweaker_balance.lua:83-103`; `chaos_wastes_tweaker_dev.lua:10192-10336` | Deterministic-order buff registration protects only mod-running peers; modded ints still ride `rpc_add_buff` to vanilla peers -> strict-lookup CTD (issues #425/#426). Owner-consumed buffs: apply via local `BuffExtension.add_buff` (never `BuffSystem.add_buff`) so nothing hits the wire. Buffs that must sync: behind the WS1.5 `all_peers_have()` parity gate, never toggle-coupled (BUG_CLASSES §31). Include the crt trn/BR stub-buff audit (do their ids ever encode onto networked buff paths? - the last unaudited issue-#371 axis). (in flight: fix-425-crt) | `buff_system.lua:302-308` (int encode); `network_lookup.lua:38-47`, `:2360-2367`; `buff_extension.lua:165` (local add) |
| P0 | gt_dev | `general_tweaker_dev/scripts/mods/general_tweaker_dev/_gt_creature_spawner.lua:835-836` | Unguarded health-ext fetch + `is_invincible = true` on all `PLAYER_AND_BOT_UNITS` with no restore outside dlc_castle -> permanent godmode leak + hot-join nil crash. Guard the fetch; mirror `is_invincible = false` in the `run_on_death` hook at `:849`. | `script_unit.lua:43-47` |
| P0 | cosmetics_tweaker | `cosmetics_tweaker/scripts/mods/cosmetics_tweaker/_tpe.lua:219-221` | `get_world()` calls `Managers.world:world("level_world")` with no `has_world` probe, and TPE `destroy_unit` cleanup can run between states -> fassert / issue-#459-class failure in a shipped public mod. Reuse the mod's own gated `_level_world()` helper (`cosmetics_tweaker.lua:6789-6796`) and release TPE units on `("exit","StateIngame")`. | `world_manager.lua:111-115` (`world()` fasserts on missing name); `game_state_machine.lua:16-21` (exit fires before teardown) |
| P0 | gt (stable - PROMOTION item, stable is read-only) | `general_tweaker/scripts/mods/general_tweaker/_gt_bot_teleport_lab.lua:868` + `:897`, `_gt_solo_qol.lua:326` | Public stable gt still carries (a) pre-#459 ungated `world("level_world")` lookups and (b) `create_screen_gui` with the font material `materials/fonts/arial` (`FONT_MTRL` at `:849`) - a guaranteed C-fatal when the bot-lab GUI spins up. Both fixed in gt_dev (has_world + identity gates; gw_fonts create at `_gt_bot_teleport_lab.lua:1100`). Surface for the next user-triggered gt promotion; run `tools/promote/promotion-status.ps1`. (in flight: fix-459-gtdev) | `world_manager.lua:111-115`; vanilla `debug.lua:12` + `:81` (arial is a `Gui.text` font arg, not a create material) |

## P1 - architecture / maintainability

| Pri | Owner mod | Our file:line | Change | Engine citation |
|---|---|---|---|---|
| P1 | ct_dev, cwv, cosmetics, cim_dev (WS5) | `docs/OOP_REFACTOR_PLAN.md:95-110` (god files: ct_dev 14,328 lines; cwv 11,808; cosmetics 10,499; cim_dev 8,173) | Decompositions currently produce `mod._ns` function bags (evt pilot). For STATEFUL subsystems use engine `class()`: `local Foo = class(Foo)` per module (gut precedent `gui_tweaker/scripts/mods/gui_tweaker/_mod_tweaker_view.lua:23`); global class ONLY when an engine registry resolves it by name. Instance state on `self` escapes the 200-locals ceiling (BUG_CLASSES §11) and gains `delete()`/`destroy()` lifecycle. | `class.lua:23-49` (idempotent), `:42-48` (delete); name resolution `script_unit.lua:99`, `hero_view_state_overview.lua:308` |
| P1 | gt / ct / gut (WS4) | `general_tweaker.lua:1958-1976` vs `chaos_wastes_tweaker.lua:7282` vs `gui_tweaker_dev/_gut_damage_numbers.lua:87` | Three divergent hand-rolled `Managers.state.event` liveness checks. Extract gt's pointer-compare re-register + named-method + pcall fan-out dispatcher as `tools/shared_lib/_lib_state_event.lua` so every mod gets the one correct handling of the rebuilt-per-transition manager and the one-registration-per-(object,event) collision. | `state_ingame.lua:2207-2209` (manager rebuilt); `event_manager.lua:16` (method-name string), `:46-48` (persistent passthrough) |
| P1 | tools/mod-lint | `tools/mod-lint/lint-mod.ps1` (new rule) | Derived-class hook trap is guarded only by prose. Generate the base->derived map from the decompile (`grep '^\w+ = class(\w+, \w+)'`, e.g. `menu_world_previewer.lua:7`) and WARN when a mod hooks a BASE class without also hooking each derived class. Would have caught wt v0.12.16 (BUG_CLASSES §1b). | `class.lua:51-57` (definition-time method copy, no `__index` chain) |
| P1 | tools/mod-lint | `tools/mod-lint` (new rule) | Lint WARNING (not error) for single-expression `ScriptUnit.extension(...):method()` derefs with no intervening nil check - the shape behind multiple teardown-window findings above. Warning because vanilla-parity ports can legitimately inherit vanilla's unguarded fetches. | `script_unit.lua:43-47` (silent nil) |
| P1 | docs + ct_dev | `docs/BUG_CLASSES.md` (catalog ends at §31) | Add TWO entries: (a) phase-1 `init` raise -> `extensions_ready` skipped -> half-initialized extension updates (the issue #470 class; greppable signature, fix at `chaos_wastes_tweaker_dev.lua:3227-3270`; verify the backfill rides the next ct dev->stable promotion); (b) static-vs-method hook arity silent arg shift (doc 07 §4.5; live P0 instance above; cite SpecialsPacing dispatch tables / `TerrorEventMixer.start_event` and evt's `_hook_wrap_table` as the correct pattern). | `entity_manager2.lua:116-171` (no pcall between phases); `mutator_handler.lua:748` + `main_path_spawning_generator.lua:327` |
| P1 | cwv | `character_weapon_variants.lua:4735,4835,5428,6301` | Unit-keyed registries with pcall-wrapped `mark_for_deletion` cleanup and no destruction hook. Register `UnitSpawner.add_destroy_listener` (fires BEFORE extension teardown; zero uses repo-wide today) at insert time; namespace the listener id with the mod id. | `unit_spawner.lua:509-521`, invoked `:265` |
| P1 | cosmetics_tweaker | `cosmetics_tweaker/scripts/mods/cosmetics_tweaker/_tpe.lua:516-528` | Four hook_safe destroy hooks (Simple/Husk InventoryExtension x destroy_slot/destroy) reap linked 3P units. Replace with one `add_destroy_listener(owner_unit, id, cb)` per owner at spawn_3p time - quarter the hook surface, cover every listener-running deletion path; keep the weak-set sweep as the level-exit backstop (listeners do not run on level-exit teardown). | `unit_spawner.lua:509`; `state_ingame.lua:1924-1928` |
| P1 | gt_dev | `general_tweaker_dev/scripts/mods/general_tweaker_dev/general_tweaker_dev.lua:1832` | Regression test `gt_dh_no_position_lookup_reads` plain-text-finds `POSITION_LOOKUP[` and matches the ban-rationale COMMENT at `_gt_debug_highlights.lua:325` -> false-fails whenever the source is readable, silently skips otherwise; it can never pass, masking real draw-path regressions. Strip `--` comment lines before scanning. [static analysis; confirm via in-game regression run] | (our tooling; ban rationale = doc 04 §4.2) |
| P1 | gt_dev | `general_tweaker_dev/scripts/mods/general_tweaker_dev/_gt_ai_takeover.lua:182-186` | `player:despawn()` + `remove_player` opens the engine's 1-frame POSITION_LOOKUP-nil window for EVERY player-unit extension; only 3 crash-discovered readers are guarded (`general_tweaker_dev.lua:531`, `_gt_hacks.lua:507`, `:523`). One audit pass over `unit_extension_templates.lua:8-113` self_owned extensions for unguarded `POSITION_LOOKUP` derefs closes the class proactively. Each guard protects only the patched peer. | `entity_manager2.lua:334` (nil at unregister); `unit_extension_templates.lua:8-113` |
| P1 | all NetworkLookup injectors | `character_weapon_variants.lua:7507,9393`; `weapons_of_chaos.lua:257`; `career_tweaker_tourney.lua:37`; `_moreitemslibrary_embedded.lua:272` | Runtime NetworkLookup appends never re-check the wire-type bit budget the engine fasserts at boot. Shared bound check `idx <= Network.type_info(<wire_type>).max` at inject time; refuse + printf ALERT on overflow. | `network_constants.lua:5-16`, `:50-63` (boot-only checks) |
| P1 | cwv / WOC | `character_weapon_variants.lua:9392-9396`; `weapons_of_chaos.lua:256-260` | Direct `item_names` injectors do not mirror `damage_sources` (MIL does, `_moreitemslibrary_embedded.lua:279-280`); safe only because clones keep `entry.name = base_weapon`. Add a regression assert: every runtime-registered item keeps a vanilla `name` OR is mirrored into `damage_sources`. | `network_lookup.lua:325` (item_names appended into damage_sources at boot) |
| P1 | cwv + tools/shared_lib | `character_weapon_variants/_lib_peer_parity.lua:287-293` (master `tools/shared_lib/_lib_peer_parity.lua:289`) | `install()` rewraps `mod.update` and its `pcall(prev, dt)` silently swallows the prior chain's errors (BUG_CLASSES §8 + invisible failure); the "host with own mod.update must call `inst:tick(dt)`" contract is comment-only. Adopt gt_dev's logged registry pattern (`general_tweaker_dev.lua:336-352`) or enforce the contract via lint/regression check. | `boot.lua:748-750` (mod.update dispatch point) |
| P1 | cwv | `character_weapon_variants.lua:6992-6993` | `/cwv_give_javelin` re-loads the held unit + `_3p` per command invocation, ungated -> +2 refs per use, unbounded in principle. `has_loaded(path, ref)` gate or a one-shot `_loaded` latch like the axe-shield block (`:4441-4444`). | `package_manager.lua:26-27` |
| P1 | cwv | `character_weapon_variants.lua:10080-10105` (resolver), `:4750-4786` (husk branch) | Husk re-key declines native-wieldable (base, career) pairs, so `cwv_es_musket` never renders its mesh/transforms/bayonet on clients (issue #474 symptoms 1+3). Ship the per-wearer variant marker over VMF mod-to-mod network (absence-safe, `_lib_peer_parity.lua:26-33` pattern) consumed in the existing husk branch as the higher-priority signal. NOTE the doc 06 vs doc 11 contradiction on fix direction (marker vs skin-key-primary resolution) - reconcile with fix-cwv-display before implementing. (in flight: fix-cwv-display) | `simple_husk_inventory_extension.lua:186` (husk stores BASE entry); wire carries name+skin only, `simple_inventory_extension.lua:255-264` |
| P1 | cwv | `character_weapon_variants.lua:10600-10628` (preview `Material.set_texture` triples), `:4820-4821` (owner path) | Old Musket textures bound via the banned `Material.set_texture` per path; illusion-browser path broken (issue #227). Register a `MaterialSettingsTemplates` entry (type=texture) and set `material_settings_name` on the cwv skin entry - vanilla applies it on owner + husk, preview and browser automatically (one data change replaces 3 bespoke apply sites; also issue #420). Mod textures must be resident (`Application.can_get` gate at `gear_utils.lua:149`). | `gear_utils.lua:149-151,195-199`; `world_hero_previewer.lua:683/706`; `loot_item_unit_previewer.lua:270` |
| P1 | cwv | `character_weapon_variants.lua:4741+` (attach), `:5033-5094` (`_wield_slot`), `:5104-5145` (visibility), `:5150-5162` (destroy_wielded) | Musket bayonet lifecycle hand-rolled across 5 hooks. Use item_template `first_person_attached_units` / `third_person_attached_units` - the engine spawns on wield, despawns, swaps per camera - deleting ~200 lines + 2 hook registrations. Verify node placement in-game; husk attach still gated on husk template resolution (row above). | `simple_inventory_extension.lua:953-969`; `attachment_utils.lua:47` |
| P1 | cosmetics + cwv + wt | `cosmetics_tweaker.lua:5029-5042` (`_offset_units`; plus `_scale_units`/`_apply_unit_path_scale_hand`) | Transform math is re-implemented per mod (issue #420) while cwv owns the guarded WA module (`character_weapon_variants.lua:9913-9981`). Promote WA to `tools/shared_lib/_lib_weapon_appearance.lua` (copied-lib pattern, precedent `_lib_peer_parity.lua:4-13`) and consume from cosmetics + wt - one guarded implementation, kills additive-offset double-apply risk. | (repo shared-lib pattern; engine transforms applied at spawn seams, doc 06 §3.2) |
| P1 | enemy_tweaker | `enemy_tweaker/scripts/mods/enemy_tweaker/enemy_tweaker.lua:983-1033` | Roaming density via global `SizeOfInterestPoint` mutation + canonical-size snap (plateaus at 8, dodges the `BreedPacksBySize` hole). Register a MutatorTemplate with `tweak_pack_spawning_settings` using `MutatorUtils.tweak_pack_spawning_settings_density_multiplier`, folded per zone - continuous per-zone scaling, zero `BreedPacksBySize` crash surface, deletes the backup/restore pair. | `mutator_utils.lua:170`; `main_path_spawning_generator.lua:327`; pattern `mutator_deus_more_roamers.lua:10-12` |
| P1 | ct_dev | `chaos_wastes_tweaker_dev.lua:2364-2413` | Progressive difficulty makes rank 8 reachable where vanilla CW never goes; only `scripts/settings/mutators/*` and the journey recorder were swept for rank-8 holes. Audit remaining DLC rank-keyed tables (grep `CATACLYSM_3`-style local constants and `get_difficulty_rank()` indexing) before widening the ramp - closes the residual issue-#470-class surface. | `conflict_director.lua:1947-1948` (unguarded `max_health[rank]`); `difficulty_settings.lua:287` (rank 8) |
| P1 | gut_dev | `gui_tweaker_dev.lua:1316` (hook), `:1084-1089` (bail comment) | Timing-fragile `IngameUI.setup_views` hook_safe attach silently bails post-Versus, leaving lazy transition-time attach as the only reliable path. Register a synthetic `DLCSettings.<slot>.ui_views` entry instead - `views_function` instantiates the view on EVERY IngameUI init with dupe fasserts and inn/mechanism filters; keep the `package.loaded` transitions mutation (that merge ran at boot). | `ingame_ui_settings.lua:731-767` (`:745-764` DLC iteration) |
| P1 | gut_dev | `gui_tweaker_dev/scripts/mods/gui_tweaker_dev/hb/mod_events.lua:92-97` | `mod.hb_update` iterates `update_funcs` with no per-consumer isolation - one HB feature error kills all later ones. pcall each update_func + log the failure, matching gt_dev registry semantics (`general_tweaker_dev.lua:336-352`). | `boot.lua:748-750` (all of it rides mod.update) |
| P1 | gt_dev (+stable copy at promotion) | `general_tweaker_dev/scripts/mods/general_tweaker_dev/_gt_creature_spawner.lua:342` | `_gt_cs_position_at_cursor` does ungated `world("level_world")` then `World.get_data` - a spawn command fired outside StateIngame fasserts. `has_world` probe + friendly echo. | `world_manager.lua:107-115` |
| P1 | cwv | `character_weapon_variants.lua:6254, :11101, :11205` | Three ungated `Managers.world:world("level_world")` sites (spawn hook + two chat commands) -> fassert outside missions. One shared `mod._cwv_level_world()` has_world helper. | `world_manager.lua:107-115` |
| P1 | repo-wide doctrine | multiple mods' `on_game_state_changed` handlers | Mods arm on `("enter","StateIngame")` but almost none use `("exit","StateIngame")` - which fires BEFORE `StateIngame.on_exit` teardown - as the primary release point for Lua-owned world resources (LineObjects, screen GUIs, spawned units, cached handles). Adopting exit-release removes whole issue-#459-class defensive code from update paths. This does **not** apply to renderer-backed package graphs: #282 proves neither this notification nor the later `StateIngame.on_exit` post-hook is a native renderer-retirement fence; those require an independently proven consumer boundary or bounded session ownership. | `game_state_machine.lua:16-21`; BUG_CLASSES §35 |
| P1 | ct_dev | `chaos_wastes_tweaker_dev.lua:11136-11138` | `ct_meta_health` stacks are local-only buffs but `max_health` is recomputed on the SERVER each tick - silent no-op under a non-ct host. Route through `BuffSystem.add_buff_synced` behind the issue-#426 parity gate, or document "all peers need ct" on the toggle. | `player_unit_health_extension.lua:310-319`; `buff_system.lua:849` |
| P1 | cim_dev + gut_dev | `crafting_in_modded_dev/scripts/mods/crafting_in_modded_dev/standard_forge.lua:508-520` | Salvage clears cim's own layers but leaves the salvaged bid in gut's native_loadouts store forever (per-read fallback masks it). Expose a gut `clear_bid(bid)` API and call it from salvage so the stores converge instead of permanently carrying dead ids. | (store design `_gut_native_loadouts.lua:62-77` - non-destructive by design) |
| P1 | gut_dev | `_gut_native_loadouts.lua:711-714` | Career-nil `set_career_read_only_data` passes through in modded; that is the hero_attributes `career`/`bot_career` write, which lands in `_characters_data` and pushes to official cloud via the UN-gated `updateHeroAttributes` (issue #402 isolation). Capture modded-side or document as a deliberate exception (values currently benign). | `playfab_mirror_base.lua:2875-2916` (characters-data push not EAC-gated); `backend_interface_hero_attributes_playfab.lua:100-101` |
| P1 | cim_dev | `crafting_in_modded_dev.lua:484-502` | `_forge_create_item` hand-rolls JSON with `tostring(v)` while `_athanor_inject_item` uses `cjson.encode` (`:5300-5303`); a string-valued property would emit invalid JSON that the mirror's `_update_data` cjson.decode rejects (crafted-item data loss). Unify on cjson. | `playfab_mirror_base.lua:1723-1787` (CustomData cjson decode) |
| P1 | repo doctrine (cosmetics + cwv) | `character_weapon_variants.lua:3938` (string-form BackendUtils hook) vs `cosmetics_tweaker.lua:4040-4045` (comment: string-form cannot resolve plain tables) | Contradictory hook-form doctrine (also vs repo `CLAUDE.md` "Hooking"); VMF actually resolves strings via `rawget(_G, name)` with deferred retry. Pick ONE repo-wide doctrine, root-cause the v0.8.58 CosmeticUtils burn (likely registration timing, not string-form), and document the LA clone-backend re-dispatch as the real constraint - prevents a future session from breaking a working hook while "fixing" its form. | VMF `hooks.lua:46-58`, `:379-389` [per doc 06]; `docs/CROSS_MOD_ARCHITECTURE.md` (LA bridge) |

## P2 - polish

| Pri | Owner mod | Our file:line | Change | Engine citation |
|---|---|---|---|---|
| P2 | multi (WS4) | `enemy_tweaker.lua:423`; `gui_tweaker/_mod_tweaker_definitions.lua:38`; `gui_tweaker_dev/_gut_native_loadouts.lua:126,829`; `general_tweaker/_gt_bot_fixes.lua:58`; `cosmetics_tweaker.lua:2230`; `weapon_tweaker/wt_dev_anim_picker.lua:1309` | 6+ hand-rolled `_deep_copy`/`_shallow_copy`/`_is_in` helpers -> engine `table.clone(t, true)` / `table.shallow_copy(t, true)` / `table.contains`, folded into a WS4 `_lib_table`. KEEP the cycle-safe `_gt_cs_deepcopy` (`_gt_creature_spawner.lua:311`) - engine clone has no cycle guard and asserts on metatables. | `foundation/scripts/util/table.lua:31/51/265`, `:37` (metatable assert) |
| P2 | 6 mods (WS6) | `docs/OOP_REFACTOR_PLAN.md:114` | Bare `_G` globals `_MEM_PROBE_T0_*` -> namespace under `mod.` per WS6; the engine's own discipline is `X = X or ...` / `rawget(_G, ...)`. | `frame_table.lua:3-5`; `script_unit.lua:5-11` |
| P2 | docs | `docs/VMF_RECIPES.md` section 2a canonical example | Hand-rolled `select('#', ...)` nil-hole capture at each new hook site -> engine `table.pack` returning `(args, n)` (count is a SECOND RETURN, not `.n`). Adopt in new wrappers + the recipe; no retrofit sweep. | `foundation/scripts/util/table.lua:210-214` |
| P2 | repo doctrine | new per-spawn features | Ad-hoc `mod.update` pollers for spawn timing -> route through the two canonical patterns: `_la_persistence.lua:256-273` next-frame queue (player wiring) and `general_tweaker.lua:290-312` bounded reapply timer. Avoids BUG_CLASSES §8 rewrap risk and re-derived timing bugs. | `Managers.player:owner(unit)` unwired at extensions_ready (doc 02 §3) |
| P2 | cim_dev | `crafting_in_modded_dev.lua:878-882` | Unknown item id currently drops the whole `rpc_sync_loadout_slot` (stale inspect-panel slot; safe). Optionally decode to the reserved `"n/a"` sentinel (index 1 by construction) as a placeholder. | `network_lookup.lua:250-252` |
| P2 | repo-wide | vanilla `network_unit.lua:56-62` | `NetworkUnit.owner_peer_id` getter is broken in vanilla (setter writes `.owner`, getter reads `.peer_id` -> always nil); zero call sites in our code (grep-verified 2026-07-11). Keep it that way - doc'd never-use; resolve owners via `Managers.player:owner(unit)` / unit_storage. Optional lint. | `network_unit.lua:56-62` |
| P2 | enemy_tweaker | `enemy_tweaker.lua:1149` | In-place `Breeds[champion].max_health` mutation is safe only because the champion is not freezable; pooled breeds resurrect with first-spawn `unmodified_max_health`. Bake a not-freezable check (or an accept-stale-pool note) into any future breed-stat toggle before copying the pattern to storm_vermin / chaos_warrior. | `breed_freezer.lua:29-69` (pool list); `generic_health_extension.lua:90-105` |
| P2 | enemy_tweaker | `enemy_tweaker.lua:1287` | `spawn_queued_unit` breed-swap hook does not see `ConflictDirector.spawn_unit_immediate` (vanilla routes only pickup-breeds there). Add a one-line comment naming the benign bypass; do NOT add a second hook (duplicate-hook / double-swap risk). | `conflict_director.lua:1893`; `pickups.lua:147,179` |
| P2 | gt_dev | `general_tweaker_dev/_gt_bot_fixes.lua:292-294` | Position helper returns `p or POSITION_LOOKUP[target_unit]` raw, pushing nil-checking onto every caller. Move the repo guard shape (`unit and ALIVE[unit] and POSITION_LOOKUP[unit]`) into the helper so the despawn-window nil is unrepresentable for all BT-action callers. | `entity_manager2.lua:334` (nil window) |
| P2 | cosmetics_tweaker | `cosmetics_tweaker.lua:2080-2090` | Boot-time bulk preload of 74 offhand packages uses SYNC loads (blocks the frame each). `async=true` for the boot bulk pass like vanilla bulk loaders; keep sync only for the just-before-Apply on-demand path. Retention + dedupe already correct. | `package_manager.lua:80-86` (sync blocks); `level_transition_handler.lua:519-529` |
| P2 | wt + cwv (guard note) | `weapon_tweaker.lua:3829/3866/3920/3987`; `character_weapon_variants.lua:4338/4367/4444/4547/5782` | NO CHANGE: one-shot boot force-loads with unique refs and no unloads are legitimate session residency (the husk crash floor needs them resident) and the template to copy. This row exists to prevent a well-meaning "add unloads" regression. | `package_manager.lua:26-27`; BUG_CLASSES §27 residency floor |
| P2 | cwv | `character_weapon_variants.lua:10502-10509` | `_find_preview_slot_info` first-matches `info.name == item_name` across slots; cross-slot muskets share the inherited base name in melee AND ranged, so the wrong def/stance can resolve in preview. Key the pending def by `slot.type` captured in the `equip_item` hook (wt's `_wt_capture_preview_item_key` pattern, `weapon_tweaker.lua:6164-6174`). | `world_hero_previewer.lua:649-786` (equip_item has the slot; `_spawn_item` does not) |
| P2 | cosmetics_tweaker | `cosmetics_tweaker.lua:8000-8007` | Husk wearer resolved by scanning `Managers.player._players` comparing `player_unit`. Use `Managers.player:unit_owner(unit)` as vanilla husk code does - one call, covers bots, no private-field dependency. | `simple_husk_inventory_extension.lua:341` |
| P2 | cwv | `character_weapon_variants.lua:5083-5091, :5127-5144` | Javelin spare-spear hidden by re-fighting vanilla visibility sets in two hooks. The vanilla data field `ammo_unit_3p` decouples the 3P spare mesh at the data level on owner AND husk with zero hooks; verify projectile paths read only `ammo_unit`. | `backend_utils.lua:148`; `gear_utils.lua:161/169` |
| P2 | enemy_tweaker | `enemy_tweaker.lua:2006-2023` | `calculate_threat_value` hook_safe rescales threat then re-derives delay flags, duplicating vanilla logic (drift risk). Scale the converted `self.delay_*_threat_value` once in the existing init/refresh hooks, or scale per-breed inputs via `ConflictDirector.set_threat_value`. | `conflict_director.lua:2327-2329` (vanilla flags); `:2313` |
| P2 | enemy_tweaker | `enemy_tweaker.lua:2031-2047` | `Pacing.update` hook_safe multiplies `total_intensity` every pacing tick. Scale `CurrentPacing.peak_intensity_threshold`/`peak_fade_threshold` at refresh time instead - the same lever vanilla's difficulty overrides use, one fewer recurring hook. | `difficulty_settings.lua:77-79` (`pacing_overrides`) |
| P2 | enemy_tweaker | `enemy_tweaker.lua:598-618, :665-675` | Horde preset swap mutates boot-global `HordeCompositionsPacing` with backup/restore while size scaling already targets the live `CurrentHordeSettings` clone (`:849-868`). Apply preset variants to the clone in the same refresh hook (rebuilding `loaded_probs` as now) - globals become read-only, restore path dies. | `conflict_director.lua:877-883` (per-refresh deep clones); `conflict_settings.lua:636-641` (loaded_probs) |
| P2 | enemy_tweaker | `enemy_tweaker.lua:1319-1339` | Roaming-elite substitution re-implements what `set_breed_override_lookup` provides. Move roamer swaps to the lookup (chance tables, no per-spawn hook cost); keep the `spawn_queued_unit` wrapper for monster/event spawns. Verify the lookup covers EnemyRecycler packs first [unverified]. | `conflict_director.lua:3331`; `ai_interest_point_system.lua:841`; pattern `mutator_elite_run.lua:19-20` |
| P2 | cim_dev | `crafting_in_modded_dev/standard_forge.lua:1730` (gate `:1725`) | Permanently-disabled (`_STD_FORGE_BTNS_ENABLED = false`) per-frame `HeroWindowCrafting.update` hook - dead per-frame cost today, and the exact child-window widget-timing trap (burned cim v0.7.57) if re-enabled. Delete the hook or re-route the driver through the parent state's `update` (`crafting_in_modded_dev.lua:4078` pattern). | `hero_view_state_overview.lua:477-480` (child update precedes widget creation) |
| P2 | cosmetics_tweaker | `cosmetics_tweaker.lua:9575-9581` | `_glow_hook_trace` fires `mod:echo` on first fire of five UI window hooks - chat spam in a shipped public mod (and `mod:info` is invisible with logging off). Convert to a `printf` one-shot. | (repo NON-NEGOTIABLE 9; BUG_CLASSES §17) |
| P2 | gut_dev | `gui_tweaker_dev/_mod_tweaker_view.lua:64-69` | Bare pcall swallows `create_input_service` failure; `get_service` then returns nil and the view opens dead with no log. printf the failure and fall back to the existing `"ingame_menu"` service, mirroring vanilla's FAKE_INPUT_SERVICE degradation. | `input_manager.lua:788-794`; `ingame_ui.lua:89-92`; `hero_view.lua:107-109` |
| P2 | repo doctrine | `gui_tweaker_dev/_gut_gui_material_guard.lua:298` | The inject-when-resident / drop-unloadable `UIRenderer.create` guard lives only in gut; hooks from different mods chain, so any future mid-mission keep-view feature in another mod must copy the module WHOLESALE (self-test interlock included) - a layer-1-only or layer-2-only port is a known non-fix (BUG_CLASSES §23 "ship both"). Do when touched. | `ui_renderer.lua:246-251` (create funnels materials); `ingame_ui_settings.lua:582-614` (keep-only groups) |
| P2 | crt | `career_tweaker_balance.lua:112-121` | Global `BuffTemplates[buff].buffs[1][field]` mutation for buffs crt's OWN procs add. Use the engine's per-application channel `params.external_optional_*` in new proc code - no restore bookkeeping, no cross-career bleed. (Vanilla-applied talent buffs: template mutation remains the only lever.) | `buff_extension.lua:240-251` |
| P2 | crt | `career_tweaker_balance.lua:36-38` | Zero-sub-buff stub `{buffs = {}}` skips `_buff_id_refs` bookkeeping. If future code removes or counts these placeholder IDs, replace the shape with one no-op stat sub-buff so it exercises the normal add/remove path. | `buff_extension.lua:493-498`, `:861`, `:889` |
| P2 | ct_dev | `chaos_wastes_tweaker_dev.lua:12114` | Kill-heal boon uses `health_regen`: fills green HP but never clears wounded state - semantic gap. Confirm intended behavior with the user first; if wounded-clear is wanted, server-side `StatusUtils.set_wounded_network`. | `generic_status_extension.lua:2247, :2250-2252`; `damage_utils.lua:2546` |
| P2 | future BR / any custom stat buff | `_archive/buff_tweaker_v0.1.12-alpha/.../buff_tweaker_registrations.lua:34-38` | New custom stat_buff keys must be seeded into `StatBuffApplicationMethods` BEFORE any `BuffExtension.init` snapshots buckets, i.e. with the other unconditional load-time registrations; no NetworkLookup entry needed. Prevents `pairs(nil)` in `apply_buffs_to_value` on first read. | `buff_extension.lua:42-44` (bucket snapshot), `:1391-1392` |
| P2 | cim_dev | `crafting_in_modded_dev.lua:729-751, :1543-1576` | Dormant loadout capture/restore machinery (force-off since 2026-06-30) still installs live BackendUtils/interface hooks that do nothing. Excise per its own comment now that gut owns loadouts - removes dead hook surface and the NON-NEGOTIABLE-8 collision risk on `set_loadout_item`. | `backend_utils.lua:22-28` (the contested seam) |
